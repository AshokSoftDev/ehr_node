// Billing Models: Invoice, InvoiceItem, Receipt, PatientAdvance

/// Invoice — Billing invoice for a patient (optionally tied to a visit)
model Invoice {
  /// Unique auto-incremented invoice identifier
  invoice_id     Int    @id @default(autoincrement())
  /// Invoice number (unique, e.g., "INV-0001")
  invoice_number String @unique
  /// FK to Patient
  patient_id     Int
  /// FK to Visit (optional — invoices can exist without a visit)
  visit_id       Int?

  // Totals (calculated)
  /// Sum of all line item amounts before discounts
  gross_total     Decimal @default(0)
  /// Discount type: "percentage" or "fixed"
  discount_type   String  @default("percentage")
  /// Discount value (percentage or fixed amount)
  discount_value  Decimal @default(0)
  /// Calculated discount amount in currency
  discount_amount Decimal @default(0)
  /// Calculated tax amount
  tax_amount      Decimal @default(0)
  /// Final total after discounts and tax
  net_total       Decimal @default(0)
  /// Total amount paid so far (sum of all receipts/payments)
  paid_amount     Decimal @default(0)
  /// Remaining balance (net_total - paid_amount)
  balance_amount  Decimal @default(0)

  // Additional fields
  /// Applied coupon code (optional)
  coupon_code            String?
  /// Calculated discount from coupon
  coupon_discount_amount Decimal   @default(0)
  /// Reason for applying discount
  discount_reason        String?
  /// Invoice issue date
  invoice_date           DateTime  @default(now())
  /// Payment due date (optional)
  due_date               DateTime?
  /// Additional invoice notes
  notes                  String?

  /// Invoice workflow status: "draft", "sent", "partial", "paid", "cancelled"
  status String @default("draft")

  /// Timestamp when the record was created
  createdAt DateTime  @default(now())
  /// User ID who created this record
  createdBy String?
  /// Timestamp of last update
  updatedAt DateTime  @updatedAt
  /// User ID who last updated this record
  updatedBy String?
  /// Soft-delete timestamp (null = not deleted)
  deletedAt DateTime?
  /// User ID who soft-deleted this record
  deletedBy String?

  // Relations
  patient  Patient       @relation(fields: [patient_id], references: [patient_id])
  visit    Visit?        @relation(fields: [visit_id], references: [visit_id])
  items    InvoiceItem[]
  receipts Receipt[]

  @@index([patient_id])
  @@index([visit_id])
  @@index([invoice_number])
  @@index([status])
  @@index([invoice_date])
  @@index([createdAt])
  @@index([deletedAt])
}

/// InvoiceItem — Line item on an invoice
model InvoiceItem {
  /// Unique auto-incremented item identifier
  item_id    Int @id @default(autoincrement())
  /// FK to parent Invoice
  invoice_id Int

  /// Item category: "drug", "procedure", "package", or "custom"
  item_type    String
  /// Display name of the item
  item_name    String
  /// FK to source master record (drug_id, procedure_id, etc.) — optional for custom items
  reference_id Int?

  // Pricing
  /// Quantity of this item
  quantity        Int     @default(1)
  /// Unit price per item
  unit_amount     Decimal @default(0)
  /// Premium/markup amount
  premium         Decimal @default(0)
  /// Line-item discount type: "percentage" or "fixed"
  discount_type   String  @default("percentage")
  /// Line-item discount value
  discount_value  Decimal @default(0)
  /// Calculated line-item discount amount
  discount_amount Decimal @default(0)
  /// Whether tax applies to this item
  tax_applicable  Boolean @default(false)
  /// Calculated net amount for this line item
  net_amount      Decimal @default(0)

  /// Additional notes for this line item
  notes         String?
  /// User/staff assigned to this item (optional)
  assigned_user String?

  /// Timestamp when the record was created
  createdAt DateTime @default(now())
  /// Timestamp of last update
  updatedAt DateTime @updatedAt

  // Relations
  invoice Invoice @relation(fields: [invoice_id], references: [invoice_id], onDelete: Cascade)

  @@index([invoice_id])
  @@index([item_type])
  @@index([reference_id])
}

/// Receipt — Payment receipt (against an invoice or standalone for advance deposits)
model Receipt {
  /// Unique auto-incremented receipt identifier
  receipt_id     Int    @id @default(autoincrement())
  /// Receipt number (unique, e.g., "RCP-0001")
  receipt_number String @unique
  /// FK to Invoice this payment is for (optional — advance deposits have no invoice)
  invoice_id     Int?
  /// FK to Patient
  patient_id     Int

  /// Payment amount
  amount         Decimal
  /// Payment method: "cash", "card", "upi", "bank_transfer", "other"
  payment_method String
  /// Receipt type: "payment", "advance_deposit", "advance_deduction"
  receipt_type   String   @default("payment")
  /// Date and time of payment
  payment_date   DateTime @default(now())
  /// Additional payment notes
  notes          String?

  /// Record status: 1 = active, 0 = inactive
  status    Int       @default(1)
  /// Timestamp when the record was created
  createdAt DateTime  @default(now())
  /// User ID who created this record
  createdBy String?
  /// Timestamp of last update
  updatedAt DateTime  @updatedAt
  /// User ID who last updated this record
  updatedBy String?
  /// Soft-delete timestamp (null = not deleted)
  deletedAt DateTime?
  /// User ID who soft-deleted this record
  deletedBy String?

  // Relations
  invoice Invoice? @relation(fields: [invoice_id], references: [invoice_id])
  patient Patient  @relation(fields: [patient_id], references: [patient_id])

  @@index([invoice_id])
  @@index([patient_id])
  @@index([receipt_number])
  @@index([receipt_type])
  @@index([status])
  @@index([payment_date])
  @@index([createdAt])
  @@index([deletedAt])
}

/// PatientAdvance — Tracks advance deposits and deductions for patient wallet
model PatientAdvance {
  /// Unique auto-incremented identifier
  advance_id       Int     @id @default(autoincrement())
  /// FK to Patient
  patient_id       Int
  /// Transaction amount (positive = deposit, negative = deduction)
  amount           Decimal
  /// Transaction type: "deposit" or "deduction"
  transaction_type String
  /// Reference type: "invoice", "manual", or null
  reference_type   String?
  /// Reference ID (e.g., invoice_id if deducted against an invoice)
  reference_id     Int?
  /// Payment method used for this transaction
  payment_method   String?
  /// Link to the receipt if one was generated
  receipt_id       Int?
  /// Additional notes
  notes            String?

  /// Record status: 1 = active, 0 = inactive
  status    Int       @default(1)
  /// Timestamp when the record was created
  createdAt DateTime  @default(now())
  /// User ID who created this record
  createdBy String?
  /// Timestamp of last update
  updatedAt DateTime  @updatedAt
  /// Soft-delete timestamp (null = not deleted)
  deletedAt DateTime?
  /// User ID who soft-deleted this record
  deletedBy String?

  // Relations
  patient Patient @relation(fields: [patient_id], references: [patient_id])

  @@index([patient_id])
  @@index([transaction_type])
  @@index([status])
  @@index([createdAt])
  @@index([deletedAt])
}
